#include<stdio.h>
#include<math.h>
void main()
{
    int num,i,p;
    printf("Enter a Number:");
    scanf("%d",&num);
    printf("Enter power:");
    scanf("%d",&p);
    i=pow(num,p);
    printf("\nresult = %d ",i);
}
